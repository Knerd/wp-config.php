WordPress configuration with local config support and composer autoload. 
Also supports setting up  wordpress in a subdirectory and using a different
wp-content directory. 

EXAMPLE
   ...
   'docroot'    => 'public',
   'wp_content' => 'wp',
   'wp_dir'     => 'wp'
